<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ie.darren_sisk.fantasyfootballdraft">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".GameRulesActivity" />
        <activity android:name=".MyTeamActivity" />
        <activity android:name=".PickPlayerActivity" />
        <activity android:name=".playerDetails" />
        <activity android:name=".playerStats" />
        <activity android:name=".PlayerGrowth" />
        <activity android:name=".PlayerAttributes" />
        <activity android:name=".CreateTeam" />
        <activity android:name=".MyLeagueTable"></activity>
    </application>

</manifest>